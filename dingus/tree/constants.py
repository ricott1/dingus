from dingus.utils import hash

LEAF_PREFIX = b"\x00"
BRANCH_PREFIX = b"\x01"
INTERNAL_LEAF_PREFIX = b"\x02"
SUBTREE_PREFIX = b"\x03"

DEFAULT_KEY_LENGTH = 32
DEFAULT_SUBTREE_HEIGHT = 4
DEFAULT_SUBTREE_NODES = 2 ** (DEFAULT_SUBTREE_HEIGHT - 1)

EMPTY_VALUE = b""
EMPTY_HASH = hash(EMPTY_VALUE)
EMPTY_HASH_PLACEHOLDER = b"\x03"

NODE_HASH_SIZE = len(EMPTY_HASH)
