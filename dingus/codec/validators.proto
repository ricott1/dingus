syntax = "proto2";

message Validator {
    required bytes blsKey = 1;
    required uint64 bftWeight = 2;
}

message Validators {
    repeated Validator validators = 1;
    required uint64 certificateThreshold = 2;
}